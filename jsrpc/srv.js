/*

const RPC = new Map, route = (name, ...wrap)=>{
  if(!RPC.has(name)) RPC.set(name, wrap)
};

[
  [
    'default',
    signed
  ],
  [
    'star',
    signed
  ]
].map(route)

返回值设计

  [
    [0,正常],
    [0,正常],
    [错误码,错误信息],
  ]

*/
